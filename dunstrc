# see `man dunst` for documentation
#
# my dunst version is 1.4.1 from 2019-07-03
#
# restart and test dunst with config
# pkill dunst && dunst -config ~/.config/dunst/dunstrc &
# notify-send testing

[global]
    font = Monospace 16
    geometry = "1000x300-50+50"
    alignment = center
    padding = 8
    markup = full
    format = "<b>%s</b>\n%b"

[urgency_normal]
    # Cyan color background
    background = "#007777"
