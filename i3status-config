general {
        output_format = "none"
        colors = false
        interval = 5
}

order += "path_exists dunst"
# order += "battery 0"
order += "path_exists VPN"
order += "volume master"
# order += "disk /"
# order += "load"
# order += "cpu_usage"
# order += "memory"
order += "time"

battery 0 {
       format = "BAT: %percentage"
       path = "/sys/class/power_supply/BAT%d/uevent"
}

path_exists VPN {
        # path exists when a VPN tunnel launched by nmcli/nm-applet is active
        path = "/proc/sys/net/ipv4/conf/tun0"
}

load {
        format = "%1min %5min %15min"
}

memory {
        format = "MEMORY: %used"
        threshold_degraded = "10%"
        format_degraded = "MEMORY: %free"
}

disk "/" {
        format = "%free"
}

volume master {
        format = "♪: %volume"
        format_muted = "♪: muted (%volume)"
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}

time {
        format = "%m-%d %H:%M:%S"
}

cpu_usage {
        format = "CPU: %usage"
        max_threshold = 99
        format_above_threshold = "CPU: HI"
}

path_exists dunst {
        # path exists when alias `notification_diable` runs
        path = "/tmp/notification_disable"
        format = "Notification: Off"
        format_down = "Notification: On"
}
