snippet script "script" b
#!/bin/bash

set -o errexit   # abort on nonzero exitstatus, same as -e
set -o nounset   # abort on unbound variable, same as -u
set -o pipefail  # don't hide errors within pipes
endsnippet
