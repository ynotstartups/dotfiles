# search file and directories starts with `.`
--hidden

# don't search in .git directory
--glob=!.git/

# don't search in standup directory
--glob=!standup/

# don't search in fixtures directory
--glob=!fixtures/

# don't let riggrep print really long lines to my terminal, and show a preview
--max-columns=150
--max-columns-preview

--smart-case
